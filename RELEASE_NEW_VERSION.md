We are about to release a new version. Here's what needs to be done:
1. Find the next tag. Check tags and top CHANGELOG.md row, we will just increase patch version by 1.
2. Update CHANGELOG.md file with changes since the last tag. Use the new tag and today's date.
3. The list of items in CHANGELOG.md are just the git summary of the merges to main. If we know the PR, also include a link. If it was done by somebody else other than @gmegidish, then include a "thanks to @user" and the user's github (and link their profile)
4. Create a branch so we can commit the CHANGELOG.md update
5. Let me review before we continue
